package test

object test4 {
  var x = new Rational(1,3)                       //> x  : test.Rational = 1/2
  var y = new Rational(5,7)                       //> y  : test.Rational = 2/3
  var z = new Rational(3,2)
  x.add(y)                                        //> res0: test.Rational = 7/6
  x.neg()                                         //> res1: test.Rational = -1/2
  x.subtract(y).subtract(z)                       //> res2: test.Rational = -1/6
}

class Rational(x: Int, y : Int) {
	var numer = x
	var denom = y
	
	def add(that : Rational) : Rational =
		new Rational(numer*that.denom + denom*that.numer, denom*that.denom)
		
	def neg() =
		new Rational(-numer, denom)
		
	def subtract(that: Rational) =
		new Rational(numer*that.denom - denom*that.numer, denom*that.denom)
		
	override def toString() =
			numer + "/" + denom
}